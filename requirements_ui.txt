# DMD GIF Converter UI — Python Dependencies
customtkinter>=5.2.2
Pillow>=10.0.0
# darkdetect 0.8.0 has a version-detection bug on macOS 15+ / 26+
# pinned to 0.7.1 to avoid the "macOS XX or later required" crash
darkdetect==0.7.1
opencv-python>=4.9.0
# Auto Action person detector — ONNX YOLOv8n runtime (CPU-only, ~6 MB model)
# Falls back gracefully to motion detection when absent
onnxruntime>=1.16.0
# GIF Search (v3.0.0)
# 'duckduckgo_search' was renamed to 'ddgs' — install both for compatibility
ddgs>=7.0.0
requests>=2.31.0
send2trash>=1.8.0
